{
  "name": "instagram-clone-api",
  "version": "1.0.0",
  "description": "JSON Server API for Instagram Clone",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}